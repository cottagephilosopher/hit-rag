# 测试 Markdown 上传

这是一个测试文档，用于验证 Markdown 文件的批量上传功能。

## 功能特性

- ✅ 跳过 MinerU 转换
- ✅ 自动触发文档处理
- ✅ 解析和切片
- ✅ 标签推理

## 测试内容

### 章节1: 介绍

这是一段测试文本，用于验证文档解析功能。系统应该能够正确识别 Markdown 的结构，包括标题、段落、列表等元素。

### 章节2: 技术细节

文档处理流程包括以下步骤:

1. 上传 MD 文件到服务器
2. 复制到 converted 目录
3. 处理图片 URL（如果有）
4. 保存到 all-md 目录
5. 自动触发文档解析
6. 生成 chunks 和标签

### 章节3: 测试数据

这里是一些测试数据，用于验证切片算法的有效性。文本应该被合理地分割成多个 chunks，每个 chunk 都有适当的长度和语义完整性。

## 总结

本文档用于测试 MD 文件的批量上传和自动处理功能。如果一切正常，这个文档应该会被正确解析、切片，并生成相应的标签。
